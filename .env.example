DATABASE_URL="file:./dev.db"
AUTH_SECRET="replace-with-a-long-random-string"

# Production should use a hosted PostgreSQL connection string instead of SQLite.
# Example:
# DATABASE_URL="postgresql://user:password@host:5432/optimize?sslmode=require"
